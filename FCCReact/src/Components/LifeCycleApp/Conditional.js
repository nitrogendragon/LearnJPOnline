import React from 'react'

export default function Conditional(props) {
    return (
        <div>
            {/* <h1>{props.isLoading ? "Loading..." : "Cool we made it" }</h1> */}
            <h1>Cool we made it</h1>
        </div>
    )
}
